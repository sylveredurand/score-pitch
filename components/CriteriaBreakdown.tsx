import { CriterionResult } from "@/types/pitch";
export function CriteriaBreakdown({ criteria }: { criteria: CriterionResult[] }) { return <section className="rounded-2xl bg-white p-6 shadow-sm"><h3 className="font-semibold">Analyse détaillée</h3><div className="mt-4 space-y-4">{criteria.map((c) => <article key={c.name} className="rounded-xl border p-4"><p className="font-medium">{c.name} — {c.score}/{c.maxScore}</p><p className="text-sm text-slate-700 mt-1">{c.comment}</p><p className="text-sm mt-1"><span className="font-medium">Conseil :</span> {c.recommendation}</p></article>)}</div></section>; }
